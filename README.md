# Modern Games - Игровая Платформа

Современный веб-сайт с регистрацией, авторизацией и отслеживанием игроков онлайн в реальном времени.

## Особенности

- 🔐 Регистрация и авторизация пользователей
- 👥 Отслеживание количества игроков онлайн в реальном времени
- 🎨 Современный дизайн в стиле Modern Games
- ⚡ WebSocket для обновлений в реальном времени
- 🔒 JWT аутентификация
- 💾 Supabase (PostgreSQL) база данных

## Установка

### 1. Настройка Supabase

1. Создайте проект на [Supabase](https://supabase.com)
2. Перейдите в **SQL Editor** в панели управления Supabase
3. Выполните SQL скрипт из файла `supabase-setup.sql` для создания таблицы `users`
4. Перейдите в **Settings** → **API** и скопируйте:
   - **Project URL** (SUPABASE_URL)
   - **anon public** ключ (SUPABASE_ANON_KEY)

### 2. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
SUPABASE_URL=your_supabase_project_url
SUPABASE_ANON_KEY=your_supabase_anon_key
JWT_SECRET=your-secret-key-change-in-production
PORT=3000
```

### 3. Установка зависимостей

```bash
npm install
```

### 4. Запуск сервера

```bash
npm start
```

Для разработки с автоматической перезагрузкой:
```bash
npm run dev
```

### 5. Откройте браузер

Перейдите на `http://localhost:3000`

## Технологии

- **Backend**: Node.js, Express
- **Frontend**: Vanilla JavaScript, HTML, CSS
- **Real-time**: Socket.io
- **База данных**: Supabase (PostgreSQL)
- **Аутентификация**: JWT (JSON Web Tokens)

## Структура проекта

```
├── server.js              # Основной серверный файл
├── package.json           # Зависимости проекта
├── supabase-setup.sql    # SQL скрипт для создания таблицы
├── .env                   # Переменные окружения (создайте сами)
├── public/               # Статические файлы
│   ├── index.html        # Главная страница
│   ├── styles.css        # Стили
│   └── app.js            # Клиентский JavaScript
└── README.md             # Документация
```

## API Endpoints

- `POST /api/register` - Регистрация нового пользователя
- `POST /api/login` - Авторизация пользователя
- `GET /api/me` - Получить информацию о текущем пользователе
- `GET /api/online-count` - Получить количество игроков онлайн

## Socket.io Events

- `user-online` - Уведомление о том, что пользователь онлайн
- `online-count` - Обновление количества игроков онлайн

## Важные замечания

⚠️ **Безопасность**: 
- Измените `JWT_SECRET` на случайную строку в продакшене
- Не коммитьте файл `.env` в репозиторий
- Для продакшена рекомендуется использовать `SUPABASE_SERVICE_ROLE_KEY` вместо `anon key` и настроить правильные RLS политики
